---
interface Work {
  icon: string;
  label: string;
  tech?: string;
}

interface Props {
  works?: Work[];
}

const { works = [
  { icon: '🤖', label: 'AI Method Board', tech: 'Spring Boot' },
  { icon: '📁', label: 'File Manager', tech: 'Tauri + Rust' },
  { icon: '🎮', label: 'Rhythm Game', tech: 'Three.js' },
  { icon: '📚', label: 'Learning Platform', tech: 'Next.js' },
  { icon: '⚡', label: 'CLI Tool', tech: 'Rust' },
  { icon: '🔧', label: 'Dev Tools', tech: 'TypeScript' }
] } = Astro.props;
---

<section id="works">
  <h2 class="section-title">作品・実績 (Works)</h2>

  <div class="works-grid">
    {works.map((work) => (
      <div class="work-item">
        <div class="work-icon">{work.icon}</div>
        <div class="work-label">{work.label}</div>
        {work.tech && <div class="work-tech">{work.tech}</div>}
      </div>
    ))}
  </div>
</section>
