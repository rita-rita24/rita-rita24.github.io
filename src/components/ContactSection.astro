---
interface ContactLink {
  icon: string;
  label: string;
  href: string;
}

interface Props {
  links?: ContactLink[];
}

const { links = [
  { icon: '📧', label: 'Email', href: 'mailto:example@example.com' },
  { icon: '🐙', label: 'GitHub', href: 'https://github.com/rita-rita24' },
  { icon: '🐦', label: 'X', href: 'https://x.com/Ar2tDev' }
] } = Astro.props;
---

<section id="contact">
  <h2 class="section-title">コンタクト (Contact)</h2>

  <div class="contact-grid">
    {links.map((link) => (
      <a href={link.href} class="contact-item" target="_blank" rel="noopener noreferrer">
        <span class="contact-icon">{link.icon}</span>
        <span class="contact-label">{link.label}</span>
      </a>
    ))}
  </div>
</section>
