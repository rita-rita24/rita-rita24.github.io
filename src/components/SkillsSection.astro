---
interface Skill {
  icon: string;
  name: string;
  description: string;
  detail?: string;
}

interface Props {
  tags?: string[];
  skills?: Skill[];
}

const {
  tags = ['Java', 'TypeScript', 'JavaScript', 'PHP', 'Laravel'],
  skills = [
//    {
//      icon: '🔵',
//      name: 'Backend Development',
//      description: 'Java + Spring Bootを使用したWebアプリケーション開発。RESTful API設計とDB最適化に精通。',
//      detail: '得意分野: マイクロサービス / API設計'
//    }
  ]
} = Astro.props;
---

<div class="section-container">
  <h2 class="section-title">スキル</h2>
  <section id="skills">
    <div class="tag-list">
      {tags.map((tag) => (
        <span class="tag">{tag}</span>
      ))}
    </div>

    {skills.map((skill) => (
      <details>
        <summary>{skill.icon} {skill.name}</summary>
        <div class="trait-content">
          {skill.description}
          {skill.detail && (
            <><br /><span style="color: var(--secondary);">{skill.detail}</span></>
          )}
        </div>
      </details>
    ))}
  </section>
</div>
